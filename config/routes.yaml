controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

app_logout:
            path: /logout
            methods: GET

change_locale:
    path: /change-locale/{locale}
    controller: App\Controller\LocaleController::change
