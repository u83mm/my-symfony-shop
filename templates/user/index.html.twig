{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block main %}
    <h1>User index</h1>

    <div class="table-responsive">
        <table class="table table-bordered table-striped">
            <thead class="text-center">
                <tr>
                    <th>Id</th>
                    <th>Email</th>
                    <th>Roles</th>                    
                    <th>Name</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr class="align-middle">
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>                    
                    <td>{{ user.name }}</td>
                    <td>
                        <a class="btn btn-outline-success" href="{{ path('app_user_show', {'id': user.id}) }}">show</a>
                        <a class="btn btn-outline-success" href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>    

    <a class="btn btn-outline-primary" href="{{ path('app_user_new') }}">Create new</a>
{% endblock %}
