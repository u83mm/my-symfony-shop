{% extends 'base.html.twig' %}

{% block title %}{{ 'edit user'|trans|capitalize }}{% endblock %}

{% block main %}
    <h1>{{ 'edit user'|trans|title }}</h1>

    <div class="row">
        <div class="col-lg-6 mx-auto">
            {{ include('user/_form.html.twig', {'button_label': 'update'|trans|capitalize}) }}
        </div>
    </div>    

    <a class="btn btn-outline-primary" href="{{ path('app_user_index') }}">{{ 'go back'|trans|capitalize }}</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
