{% extends 'base.html.twig' %}

{% block title %}Change User Password{% endblock %}

{% block main %}
    <h1>Change User Password</h1>

    <div class="row">
        <div class="col-lg-6 mx-auto">
            {{ include('user/_change_passwd_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>    

    <a class="btn btn-outline-primary" href="{{ path('app_user_edit', {'id': user.id}) }}">back to list</a>   
{% endblock %}