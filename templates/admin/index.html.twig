{% extends 'base.html.twig' %}

{% block title %}Panel Admin{% endblock %}

{% block main %}
<a class="btn btn-outline-secondary" href="{{ path('app_user_index') }}">{{ 'users'|trans|capitalize }}</a>
<a class="btn btn-outline-secondary" href="{{ path('app_product_index') }}">{{ 'products'|trans|capitalize }}</a>
{% endblock %}
